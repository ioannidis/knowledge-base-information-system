spring:
  application:
    name: infrastructureservice
  profiles:
    active:
      default
    cloud:
      config:
        enabled: true
      discovery:
        service-id: configservice
        enabled: true
