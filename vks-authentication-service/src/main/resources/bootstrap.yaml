spring:
  application:
    name: authenticationservice
  profiles:
    active:
      default
  cloud:
    config:
      enabled: true
    discovery:
      service-id: configservice
      enabled: true